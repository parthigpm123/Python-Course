/* SQL script to create a table named Studentrecords*/
CREATE TABLE Studentrecords (
    student_id INT NOT NULL,
    Student_name VARCHAR(50),
    Studentmarks INT,
    Studentdepartment VARCHAR(50)
);

CREATE TABLE cars(
    carname VARCHAR(50),
    carmodel INT,
    carprice INT,
    carowner VARCHAR(50)
);



