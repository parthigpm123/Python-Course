

CREATE TABLE Datta (
    pid SERIAL PRIMARY KEY,
    name VARCHAR(100),
    age INT,
    address VARCHAR(100),
    contact VARCHAR(100),
    mail VARCHAR(50)
);



CREATE TABLE Datta (
    pid SERIAL PRIMARY KEY,
    name VARCHAR(100),
    age INT,
    address VARCHAR(100),
    contact VARCHAR(100),
    mail VARCHAR(50)
);

CREATE TABLE Studentdatas(
    ID INT,
    NAME VARCHAR(20),
    AGE INT,
    DOB VARCHAR(20),
    GENDER VARCHAR(20),
    CITY VARCHAR(20)
);
