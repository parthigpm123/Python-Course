
/*🟠 2. TRUNCATE → remove all rows (keep table)
-- Remove everything but keep the table structure
TRUNCATE TABLE car;

-- Check table after truncate
SELECT * FROM car;


👉 Table is now empty, but carname, carprice, etc. still exist. You can insert new data again.--
-- Remove everything but keep the table structure*/




truncate table studentrecords;